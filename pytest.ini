[pytest]
# Repo-root suite: graph.json guard tests only. The per-service suites are run
# from their own directories by `circleci testsuite`, each with its own
# pytest.ini and impact-key.
testpaths = tests
python_files = test_*.py
addopts = -ra
