# worker has no third-party runtime dependencies. Its in-repo dependencies are
# services/api and packages/shared-core, wired via conftest.py (see graph.json).
# coverage >= 6.3 is required for `coverage lcov`.
pytest>=8.0,<9.0
coverage>=7.4,<8.0
